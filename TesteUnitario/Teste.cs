using System;
using Domain.Interfaces;
using Domain.Interfaces.Service;
using Service.PedidoService;
using Xunit;

namespace TesteUnitario
{
    public class Teste
    {
       /* private readonly IPedidoService _pedidoService;
        private readonly Mock<IPedidoRepository> _mock = new Mock<IPedidoRepository>();
        public Teste()
        {
            _pedidoService = new PedidoService(_mock.Object);
        }
        [Theory]
        [InlineData(0,20)]
        [InlineData(20,40)]
        [InlineData(40,60)]
        [InlineData(60,80)]
        [InlineData(80,100)]
        public void PaginacaoCorreta(int paginaI,int paginaF)
        {
            var quantidade = paginaI + paginaF;
            Assert.True(paginaI + paginaF == quantidade);
        }*/
    }
}
